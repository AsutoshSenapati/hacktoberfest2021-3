#include <bits/stdc++.h>
using namespace std;
void solve()
{
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    int x = __gcd(a, b);
    int y = __gcd(c, d);
    cout << __gcd(x, y);
}
int main()
{
    solve();
}
